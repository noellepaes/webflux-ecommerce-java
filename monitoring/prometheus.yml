global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/prometheus-rules.yml

scrape_configs:
  - job_name: ecommerce-app
    metrics_path: /actuator/prometheus
    static_configs:
      - targets: ["app:8080"]
